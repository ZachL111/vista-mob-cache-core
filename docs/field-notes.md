# Field Notes

`vista-mob-cache-core` is easiest to review by starting with the fixture, not the prose.

The domain cases cover `form pressure`, `sync drift`, `local state`, and `conflict cost`. They sit beside the smaller starter fixture so the project has both a compact scoring check and a domain-flavored review check.

The model rewards signal and confidence, then pulls back for drag. In this fixture, `baseline` shows where that pullback matters most.

The extra check gives the repository a behavior path that can fail for a domain reason, not only a syntax reason.
